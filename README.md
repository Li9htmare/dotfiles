```
bash <( \
    curl --fail --location --silent --show-error \
        https://raw.githubusercontent.com/Li9htmare/dotfiles/master/scripts/install.sh \
)
```

```
sudo chsh -s "$shell_path" "$USER"
```
